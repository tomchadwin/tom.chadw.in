/*
 * The `AbstractFrameBufferAttachment` class doesn't exist.
 * The purpose of this file is to generate the *documentation*
 * of such a class, so that the class diagram looks nicer.
 */

/**
 * @class AbstractFrameBufferAttachment
 *
 * A logical grouping of `RenderBuffer` and `Texture`.
 *
 * A `FrameBuffer` is composed of (at least) three attachments, each
 * of which can be either a `RenderBuffer` and `Texture`.
 */
